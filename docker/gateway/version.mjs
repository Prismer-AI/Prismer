/**
 * container-gateway version — Single Source of Truth
 *
 * Update this file when bumping the gateway version.
 * Then sync: docker/compatibility.json, docker/versions-manifest.json
 */
export const VERSION = '1.1.0';
